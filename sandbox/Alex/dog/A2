import os


def reNameFiles(prefix):
	counter = 1	
	for originalFileName in os.listdir():
		newFileName = prefix + str(counter)
		os.system('cp ' + originalFileName + ' ' + newFileName)
		counter += 1


reNameFiles('A')
	
